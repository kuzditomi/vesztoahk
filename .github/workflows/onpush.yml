name: On push

on: push

jobs:
  onpush:
    name: compile
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Build
        uses: ./.github/workflows/build.yml
